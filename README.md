# Learn_jwt

# Learn_jwt
### Libraries to be installed
1. pip install pyJWT
2. pip install Flask, Flask-sqlalchemy, Flask-migrate
3. pip install flasgger
4. pip install cloudinary, get api key, api secret, environmental variables

### migrations
flask db init
flask db migrate -m"initial migration"
flask db upgrade